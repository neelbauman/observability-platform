# ---------------------------------
# 可観測性スタック シークレット
# ---------------------------------

# 1. Postgres スーパーユーザー (backends.yml が使用)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_secret_pass

# 2. rclone S3 ゲートウェイ (backends.yml が使用)
RCLONE_S3_USER=obs-user
RCLONE_S3_PASS=obs-secret-password

# 3. Langfuse サービス (langfuse.yml が使用)
LANGFUSE_DB_USER=langfuse_user
LANGFUSE_DB_PASS=langfuse_secret_pass
LANGFUSE_DB_NAME=langfuse_db
LANGFUSE_NEXTAUTH_SECRET=a_very_long_random_secret_string_for_nextauth

# 4. (将来用) MLflow サービス
# MLFLOW_DB_USER=mlflow_user
# MLFLOW_DB_PASS=mlflow_secret_pass
# MLFLOW_DB_NAME=mlflow_db